@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@using EleVehicleDealer.DAL.EntityModels
@model IEnumerable<EleVehicleDealer.DAL.EntityModels.Vehicle>
@{
    var username = Context.Session?.GetString("Username") ?? string.Empty;
    var isLoggedIn = !string.IsNullOrEmpty(username);
    var accountId = Context.Session?.GetInt32("AccountId") ?? 0;
    var userRole = Context.Session?.GetString("Role") ?? "Customer"; // Lấy role từ session

    var error = ViewBag.Error as string ?? TempData["Error"] as string ?? string.Empty;
    var vehicleTypes = ViewBag.VehicleTypes as List<string> ?? new List<string>();
    var vehicleModels = ViewBag.VehicleModels as List<string> ?? new List<string>();
    var sortBy = ViewBag.SortBy as string ?? "price"; // Mặc định là "price"
    var filterType = ViewBag.FilterType as string ?? "Sedan"; // Mặc định là "Sedan"
    var filterModel = ViewBag.FilterModel as string ?? "Model X"; // Mặc định là "Model X"
    var minPrice = ViewBag.MinPrice as decimal? ?? 10000m; // Mặc định là 10000
    var maxPrice = ViewBag.MaxPrice as decimal? ?? 50000m; // Mặc định là 50000
}

<header class="site-header">
    <div class="header-container">
        <div class="logo-section">
            <a asp-controller="Home" asp-action="Index" class="logo">EleVehicle Dealer</a>
        </div>
        <nav class="main-navigation">
            <ul>
                <li><a asp-controller="Home" asp-action="Index">Home</a></li>
                <li><a asp-controller="Menu" asp-action="Index">Menu</a></li>
                <li><a asp-controller="Category" asp-action="Index">Category</a></li>
                <li><a asp-controller="Home" asp-action="About">About</a></li>
                <li><a asp-controller="Home" asp-action="Contact">Contact</a></li>
            </ul>
        </nav>
        <div class="auth-section">
            @if (isLoggedIn)
            {
                <span class="welcome">Welcome, @username</span>
                @if (userRole == "Admin" || userRole == "Staff")
                {
                    <a asp-controller="Home" asp-action="Manage" class="nav-link">Manage</a>
                }
                @if (userRole == "Admin")
                {
                    <a asp-controller="Admin" asp-action="Dashboard" class="nav-link">Admin Panel</a>
                }
                <a asp-controller="Account" asp-action="Logout" class="btn-logout">Logout</a>
            }
            else
            {
                <a asp-controller="Account" asp-action="Login" class="btn-login" id="loginLink">Login</a>
                <a asp-controller="Account" asp-action="Register" class="btn-register" id="registerLink">Register</a>
            }
        </div>
    </div>
</header>

<div class="main-container">
    @if (!string.IsNullOrEmpty(error))
    {
        <div class="alert alert-danger">@error</div>
    }

    <h2>Welcome to EleVehicle Dealer</h2>

    <form method="get" asp-controller="Home" asp-action="Home" class="advanced-filter-container" id="filterForm">
        <div class="filter-row">
            <div class="filter-group">
                <label for="sortBy">Sort By:</label>
                <select id="sortBy" name="sortBy" class="form-control" onchange="autoSubmitFilter()">
                    <option value="All">All</option>
                    <option value="alphabet">Alphabet (A-Z)</option>
                    <option value="alphabet_desc">Alphabet (Z-A)</option>
                    <option value="price">Price (Low to High)</option>
                    <option value="price_desc">Price (High to Low)</option>
                    <option value="type">Type (A-Z)</option>
                    <option value="type_desc">Type (Z-A)</option>
                </select>
                @{
                    var selectedSortBy = sortBy;
                }
                <script>
                    document.getElementById('sortBy').value = '@selectedSortBy' === 'All' ? 'All' : '@selectedSortBy';
                </script>
            </div>

            <div class="filter-group">
                <label for="filterType">Filter by Type:</label>
                <select id="filterType" name="filterType" class="form-control" onchange="autoSubmitFilter()">
                    <option value="All">All Types</option>
                    @foreach (var type in vehicleTypes)
                    {
                        <option value="@type">@type</option>
                    }
                </select>
                @{
                    var selectedFilterType = filterType;
                }
                <script>
                    document.getElementById('filterType').value = '@selectedFilterType' === 'All' ? 'All' : '@selectedFilterType';
                </script>
            </div>

            <div class="filter-group">
                <label for="filterModel">Filter by Model:</label>
                <select id="filterModel" name="filterModel" class="form-control" onchange="autoSubmitFilter()">
                    <option value="All">All Models</option>
                    @foreach (var vehicleModel in vehicleModels)
                    {
                        <option value="@vehicleModel">@vehicleModel</option>
                    }
                </select>
                @{
                    var selectedFilterModel = filterModel;
                }
                <script>
                    document.getElementById('filterModel').value = '@selectedFilterModel' === 'All' ? 'All' : '@selectedFilterModel';
                </script>
            </div>
        </div>

        <div class="filter-row">
            <div class="filter-group price-group">
                <label for="minPrice">Min Price:</label>
                <input type="number" id="minPrice" name="minPrice" class="form-control price-input"
                       placeholder="Min" value="@minPrice" step="1000" min="0" onchange="autoSubmitFilter()">
            </div>

            <div class="filter-group price-group">
                <label for="maxPrice">Max Price:</label>
                <input type="number" id="maxPrice" name="maxPrice" class="form-control price-input"
                       placeholder="Max" value="@maxPrice" step="1000" min="0" onchange="autoSubmitFilter()">
            </div>

            <div class="filter-group">
                <button type="submit" class="btn-filter" style="display:none;">Apply Filters</button>
                <button type="button" class="btn-clear" onclick="clearFilters()">Clear All</button>
            </div>
        </div>
    </form>

    <div class="display-options">
        <div class="results-count">
            <span>Showing @Model.Count() vehicles</span>
        </div>
    </div>

    <div class="vehicle-grid" id="vehicleGrid">
        @foreach (var v in Model)
        {
            <div class="vehicle-card">
                <div class="image-container">
                    <img src="@Url.Content("~/images/" + v.Model.Replace(" ", "-") + ".jpg")"
                         alt="@v.Model"
                         onerror="this.src='@Url.Content("~/images/111.jpg")'" />
                </div>
                <div class="vehicle-card-content">
                    <h4>@v.Model</h4>
                    <p><strong>Type:</strong> <span>@v.Type</span></p>
                    <p><strong>Color:</strong> <span>@v.Color</span></p>
                    <p class="price"><strong>Price:</strong> <span>@v.Price.ToString("C")</span></p>
                    <button class="btn-buy">Buy Now</button>
                </div>
            </div>
        }
    </div>
</div>

<script>
    function clearFilters() {
        document.getElementById('sortBy').value = 'All';
        document.getElementById('filterType').value = 'All';
        document.getElementById('filterModel').value = 'All';
        document.getElementById('minPrice').value = '';
        document.getElementById('maxPrice').value = '';
        document.getElementById('filterForm').submit();
    }

    function autoSubmitFilter() {
        const minPrice = parseFloat(document.getElementById('minPrice').value) || 0;
        const maxPrice = parseFloat(document.getElementById('maxPrice').value) || Infinity;

        if (minPrice > maxPrice) {
            document.getElementById('maxPrice').value = minPrice;
        }
        document.getElementById('filterForm').submit();
    }

    document.addEventListener('DOMContentLoaded', function() {
        if (document.getElementById('sortBy')) {
            document.getElementById('sortBy').addEventListener('change', autoSubmitFilter);
        }
        if (document.getElementById('filterType')) {
            document.getElementById('filterType').addEventListener('change', autoSubmitFilter);
        }
        if (document.getElementById('filterModel')) {
            document.getElementById('filterModel').addEventListener('change', autoSubmitFilter);
        }
    });

    document.getElementById('minPrice').addEventListener('change', autoSubmitFilter);
    document.getElementById('maxPrice').addEventListener('change', autoSubmitFilter);

    document.getElementById('loginLink')?.addEventListener('click', function(e) {
        console.log('Login link clicked, redirecting to /Account/Login');
    });

    document.getElementById('registerLink')?.addEventListener('click', function(e) {
        console.log('Register link clicked, redirecting to /Account/Register');
    });
</script>

<style>
    :root {
        --primary-gradient: linear-gradient(135deg, #667eea, #764ba2);
        --secondary-gradient: linear-gradient(135deg, #28a745, #20c997);
        --shadow-light: 0 4px 12px rgba(0, 0, 0, 0.1);
        --shadow-hover: 0 6px 16px rgba(0, 0, 0, 0.2);
    }

    .site-header {
        background: var(--primary-gradient);
        padding: 15px 0;
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        box-shadow: var(--shadow-light);
    }

    .header-container {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 30px;
    }

    .logo-section .logo {
        color: white;
        font-size: 2rem;
        font-weight: 700;
        text-decoration: none;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: color 0.3s ease;
    }

        .logo-section .logo:hover {
            color: #ffd700;
        }

    .main-navigation ul {
        list-style: none;
        display: flex;
        gap: 30px;
        margin: 0;
        padding: 0;
    }

    .main-navigation a {
        color: white;
        text-decoration: none;
        font-weight: 600;
        font-size: 15px;
        text-transform: uppercase;
        transition: color 0.3s ease;
    }

        .main-navigation a:hover {
            color: #ffd700;
        }

    .auth-section {
        display: flex;
        align-items: center;
        gap: 20px;
    }

        .auth-section a,
        .auth-section span {
            color: white;
            font-weight: 600;
            font-size: 15px;
        }

    .btn-login, .btn-register {
        padding: 10px 25px;
        border-radius: 25px;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 15px;
    }

    .btn-login {
        background: rgba(255, 255, 255, 0.2);
    }

        .btn-login:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
        }

    .btn-register {
        background: var(--secondary-gradient);
    }

        .btn-register:hover {
            background: linear-gradient(135deg, #218838, #1ea085);
            transform: translateY(-2px);
        }

    .btn-logout {
        padding: 10px 25px;
        border-radius: 25px;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 15px;
        background: linear-gradient(135deg, #dc3545, #c82333);
    }

        .btn-logout:hover {
            background: linear-gradient(135deg, #bd2130, #a71d2a);
            transform: translateY(-2px);
        }

    .nav-link {
        color: white;
        text-decoration: none;
        font-weight: 600;
        font-size: 15px;
        text-transform: uppercase;
        transition: color 0.3s ease;
    }

        .nav-link:hover {
            color: #ffd700;
        }

    .welcome {
        margin-right: 15px;
        font-size: 16px;
    }

    body {
        padding-top: 80px;
        background: linear-gradient(135deg, #f5f7fa 0%, #e9ecef 100%);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .main-container {
        max-width: 1400px;
        margin: 20px auto;
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 40px;
        box-shadow: var(--shadow-light);
    }

    h2 {
        text-align: center;
        color: #2c3e50;
        font-size: 2.8rem;
        font-weight: 700;
        margin-bottom: 40px;
        background: var(--primary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .alert {
        padding: 15px 25px;
        border-radius: 15px;
        margin-bottom: 30px;
        font-weight: 500;
        border: none;
        box-shadow: var(--shadow-light);
    }

    .alert-danger {
        background: linear-gradient(135deg, #ff6b6b, #ee5a52);
        color: white;
    }

    .advanced-filter-container {
        background: white;
        padding: 15px;
        border-radius: 15px;
        margin-bottom: 20px;
        box-shadow: var(--shadow-light);
        border: 1px solid rgba(255, 255, 255, 0.2);
        position: relative;
        overflow: hidden;
        transition: box-shadow 0.3s ease;
    }

        .advanced-filter-container:hover {
            box-shadow: var(--shadow-hover);
        }

    .filter-row {
        display: flex;
        align-items: center;
        gap: 15px;
        flex-wrap: wrap;
        margin-bottom: 15px;
    }

    .filter-group {
        display: flex;
        flex-direction: column;
        gap: 5px;
        min-width: 150px;
        flex: 1;
    }

        .filter-group.price-group {
            min-width: 120px;
        }

        .filter-group label {
            font-weight: 600;
            color: #2c3e50;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin: 0;
        }

    .form-control {
        padding: 8px 12px;
        border-radius: 10px;
        border: 2px solid #e1e8ed;
        font-size: 13px;
        font-weight: 500;
        color: #2c3e50;
        background: white;
        transition: all 0.3s ease;
        cursor: pointer;
    }

        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 6px rgba(102, 126, 234, 0.3);
            outline: none;
        }

    .price-input {
        cursor: text;
    }

    .btn-filter, .btn-clear {
        padding: 8px 20px;
        border: none;
        border-radius: 10px;
        font-weight: 600;
        font-size: 13px;
        cursor: pointer;
        transition: all 0.3s ease;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-right: 10px;
    }

    .btn-filter {
        background: var(--primary-gradient);
        color: white;
        box-shadow: var(--shadow-light);
    }

        .btn-filter:hover {
            background: linear-gradient(135deg, #5a6fd8, #6a4190);
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

    .btn-clear {
        background: linear-gradient(135deg, #6c757d, #495057);
        color: white;
        box-shadow: var(--shadow-light);
    }

        .btn-clear:hover {
            background: linear-gradient(135deg, #5a6268, #343a40);
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

    .display-options {
        background: white;
        padding: 15px 20px;
        border-radius: 12px;
        margin-bottom: 20px;
        box-shadow: var(--shadow-light);
        display: flex;
        justify-content: flex-end;
        align-items: center;
        flex-wrap: wrap;
        gap: 15px;
    }

    .results-count {
        color: #6c757d;
        font-weight: 600;
        font-size: 14px;
    }

        .results-count span {
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 700;
        }

    .vehicle-grid {
        display: grid;
        gap: 25px;
        margin-top: 40px;
        padding: 15px;
        grid-template-columns: repeat(4, 1fr);
        transition: all 0.3s ease;
    }

    .vehicle-card {
        background: white;
        border-radius: 15px;
        padding: 0;
        box-shadow: var(--shadow-light);
        text-align: center;
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        border: 2px solid transparent;
        overflow: hidden;
        position: relative;
    }

        .vehicle-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: var(--shadow-hover);
            border-color: rgba(102, 126, 234, 0.3);
        }

    .image-container {
        width: 100%;
        height: 250px;
        background: linear-gradient(135deg, #f8f9fa, #e9ecef);
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 15px 15px 0 0;
        margin-bottom: 0;
        position: relative;
        overflow: hidden;
        border: none;
    }

        .image-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.4s ease;
        }

    .vehicle-card:hover .image-container img {
        transform: scale(1.1);
    }

    .vehicle-card-content {
        padding: 20px 15px;
        position: relative;
        z-index: 1;
    }

    .vehicle-card h4 {
        margin: 0 0 15px 0;
        font-size: 1.5rem;
        color: #2c3e50;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
    }

    .vehicle-card p {
        margin: 10px 0;
        color: #6c757d;
        font-size: 15px;
        font-weight: 500;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 5px 0;
    }

        .vehicle-card p.price {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--secondary-gradient);
            margin-top: 20px;
            padding-top: 15px;
            border-top: 2px solid #f8f9fa;
        }

        .vehicle-card p strong {
            color: #495057;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 13px;
            letter-spacing: 0.5px;
        }

    .btn-buy {
        margin-top: 20px;
        padding: 12px 25px;
        background: var(--secondary-gradient);
        color: white;
        border: none;
        border-radius: 20px;
        cursor: pointer;
        font-weight: 600;
        font-size: 15px;
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: all 0.3s ease;
        box-shadow: var(--shadow-light);
    }

        .btn-buy:hover {
            background: linear-gradient(135deg, #218838, #1ea085);
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

        .btn-buy:active {
            transform: translateY(0);
            box-shadow: var(--shadow-light);
        }
</style>